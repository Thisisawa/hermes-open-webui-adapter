# hermes-open-webui-adapter
"Make Hermes tool calls visible in Open WebUI and Conduit app" An SSE proxy that converts Hermes's custom tool call format into non-tool type clean Markdown text to avoid rendering and more issues.
